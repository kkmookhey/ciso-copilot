# <Subsystem name> — Codebase Documentation

> Generated by codebase-documentation sweep 2026-06-05. Purpose: give a
> future spec author a grep-able answer to "does this already exist?"
> before they propose building it. Cite `file:line` everywhere.

## Overview

2–4 sentences: what this subsystem does, where it sits in the system,
what triggers it (HTTP route / EventBridge / SQS / cron / manual).

## Components

For each source file/module in scope:

### `relative/path/to/file.py`
- **Purpose:** one line.
- **Entry point / handler:** `handler(event, context)` or N/A (cite line).
- **Key symbols:** `func_name()` (line N) — one line each. Classes too.
- **Reads:** DB tables, SQS queues, S3 prefixes, env vars, external APIs.
- **Writes:** DB tables, SQS, S3, Slack, etc.
- **Calls / called by:** upstream + downstream components by name.

## Data & contracts touched

- **DB tables + columns** used (read/write), with the exact column names.
- **API routes** handled or called (METHOD + path).
- **Env vars** consumed (exact names).
- **AWS resources** (queues, buckets, tables, secrets) by logical name.
- **Event shapes** consumed/emitted (detail-type, SQS body schema).

## Dead code (suspected)

`file:line` — `symbol` — why unused. **You MUST `grep -rn` the WHOLE repo
(not just this subsystem) before claiming dead.** Mark confidence
High/Med/Low and paste the grep evidence (0 hits, only-self-reference, etc.).

## Optimization opportunities

`file:line` — issue — suggested improvement. Keep concrete (N+1 query,
repeated full-table scan, sync call in loop, missing pagination, etc.).
No speculative rewrites.

## Gotchas / non-obvious behavior

The things a future spec author MUST know to avoid re-building or breaking
this. Schema quirks, auth patterns, idempotency keys, ordering constraints,
things that already exist that people assume don't.
