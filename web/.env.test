# This file is INTENTIONALLY committed. It contains placeholder URLs only,
# used by vitest to satisfy web/src/lib/env.ts's requireEnv() guards at
# module load. Never put real values here — those live in web/.env.production
# (gitignored).
VITE_API_BASE_URL=https://api.test.local/v1
VITE_APP_DOMAIN=https://app.test.local
VITE_STREAM_BASE_URL=https://stream.test.local
